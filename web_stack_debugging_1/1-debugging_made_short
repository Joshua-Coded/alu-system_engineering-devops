#!/usr/bin/env bash
# Install and configure Nginx
apt-get update && apt-get install -y nginx
echo "listen 80;" | tee /etc/nginx/sites-available/default /etc/init.d/nginx start
service nginx status | grep -q "not running" && echo "nginx is not running"

